#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"

HOST="${HOST:-0.0.0.0}"
PORT="${PORT:-8091}"
OLLAMA_HOST="${OLLAMA_HOST:-http://127.0.0.1:11434}"
OLLAMA_MODEL="${OLLAMA_MODEL:-qwen2.5-coder:1.5b}"

OLLAMA_NUM_CTX="${OLLAMA_NUM_CTX:-4096}"
OLLAMA_NUM_PREDICT="${OLLAMA_NUM_PREDICT:-512}"
SCAN_COMMAND_MAX_OUTPUT="${SCAN_COMMAND_MAX_OUTPUT:-4000}"
FILE_SCAN_MAX_DEPTH="${FILE_SCAN_MAX_DEPTH:-4}"
FILE_SCAN_MAX_ENTRIES="${FILE_SCAN_MAX_ENTRIES:-200}"
FILE_SCAN_MAX_VISITED="${FILE_SCAN_MAX_VISITED:-2000}"

export HOST PORT OLLAMA_HOST OLLAMA_MODEL OLLAMA_NUM_CTX OLLAMA_NUM_PREDICT SCAN_COMMAND_MAX_OUTPUT FILE_SCAN_MAX_DEPTH FILE_SCAN_MAX_ENTRIES FILE_SCAN_MAX_VISITED
exec node app.js
